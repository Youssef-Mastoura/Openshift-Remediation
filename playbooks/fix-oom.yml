---
- name: Remediation - Scale Up Memory
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Patch Deployment with more memory
      kubernetes.core.k8s:
        state: patched
        kind: Deployment
        # Accessing the variable passed from the EDA rulebook
        name: "{{ event.payload.alerts[0].labels.app }}"
        namespace: "ms-demo"
        definition:
          spec:
            template:
              spec:
                containers:
                  - name: "{{ event.payload.alerts[0].labels.app }}"
                    resources:
                      limits:
                        memory: "512Mi"
